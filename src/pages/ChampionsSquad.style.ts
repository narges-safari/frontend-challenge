import { createUseStyles } from "react-jss";

const useChampionsSquadStyle = createUseStyles({
  avatar: {
    position: "fixed",
    height: 88,
    width: 88,
    top: 16,
  },
});

export default useChampionsSquadStyle;
